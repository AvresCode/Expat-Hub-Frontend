import { MyPageComponent } from '../components';
import { MainContainer } from '../styled';

export const MyPage = () => {
  return (
    <MainContainer>
      <MyPageComponent />
    </MainContainer>
  );
};
